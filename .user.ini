; .user.ini | Mesmo endurecimento do cookie de sessão do .htaccess, para
; quando o PHP roda via PHP-FPM/CGI (que ignora php_value em .htaccess,
; mas lê .user.ini). Ver .htaccess para a explicação completa.
session.cookie_samesite = "Lax"
session.cookie_httponly = 1
